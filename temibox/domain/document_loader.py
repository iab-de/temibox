from abc import ABCMeta

class DocumentLoader(metaclass=ABCMeta):
    r"""
    Placeholder for the document loader

    A document loader is an optional domain-specific pipeline step
    that loads documents from the disk and / or creates domain-specific
    documents from raw inputs
    """
    pass